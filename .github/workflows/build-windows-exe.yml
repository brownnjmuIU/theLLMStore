name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Find Streamlit static path
        shell: pwsh
        run: |
          $st_path = python -c "import streamlit; import os; print(os.path.dirname(streamlit.__file__))"
          echo "STREAMLIT_PATH=$st_path" >> $env:GITHUB_ENV

      - name: Build (onedir)
        shell: pwsh
        run: |
          pyinstaller --onedir --name LLM_Bundler `
            --collect-all streamlit `
            --collect-all fitz `
            --collect-all altair `
            --hidden-import streamlit.web.bootstrap `
            --hidden-import streamlit.runtime.scriptrunner `
            --hidden-import docx `
            --hidden-import pptx `
            --collect-all langchain_text_splitters `
            --add-data "$env:STREAMLIT_PATH/static;streamlit/static" `
            --add-data "$env:STREAMLIT_PATH/runtime;streamlit/runtime" `
            --add-data "app.py;." `
            --add-data "extractors;extractors" `
            --add-data "processing;processing" `
            --add-data "storage;storage" `
            launcher.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: LLM_Bundler_windows_build
          path: dist/LLM_Bundler